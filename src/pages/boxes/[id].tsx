import { type NextPage } from 'next'

const Boxes: NextPage = () => {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center bg-gradient-to-b">
            <h2>Boxes listed here!</h2>
        </main>
    )
}

export default Boxes
